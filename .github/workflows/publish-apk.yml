name: Publish sideload APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fetch APK
        run: |
          set -euo pipefail
          URL=$(tr -d '\n' < download-url.txt)
          curl -L --fail --retry 3 -o liuguang-1.1.0.apk "$URL"
          test "$(stat -c%s liuguang-1.1.0.apk)" -gt 1000000
          ls -lh liuguang-1.1.0.apk
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete v1.1.0 --yes || true
          gh release create v1.1.0 liuguang-1.1.0.apk \
            --title "流光 1.1.0 sideload" \
            --notes "Second-cut Android sideload APK (not on Play Store). package com.qizi.vod. kUseMock=false. kBaseUrl=https://lumen-cinema.vercel.app (no /api). Tabs: 首页 / 发现 / 历史 / 我的. Empty CMS shows designed empty states; live items render when the BFF has them."
